# ==========================================================
# Pizzaria backend — dependências reais (limpo do Emergent)
# Intervalos compatíveis com FastAPI 0.110 (versões reais do PyPI).
# ==========================================================

# Web framework / servidor
fastapi==0.110.1
uvicorn[standard]>=0.25,<0.31
python-multipart==0.0.9

# Validação de dados
pydantic>=2.6,<2.10
email-validator>=2.1

# Base de dados (MongoDB)
motor==3.3.1
pymongo>=4.5,<4.6

# Autenticação
bcrypt>=4.1,<5
PyJWT>=2.8,<3

# Configuração (.env)
python-dotenv>=1.0

# QR codes e imagens
qrcode>=7.4
pillow>=10.3,<12

# Agendador (relatório diário) + base de timezones (Europe/Lisbon)
APScheduler>=3.10,<4
tzdata>=2024.1

# Envio de email (relatório diário)
resend>=2.0
